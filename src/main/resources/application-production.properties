# Configuración para producción (Render) - ProductosService
spring.application.name=ProductosService

# Configuración del servidor
server.port=${PORT:8084}
server.address=0.0.0.0

# Configuración PostgreSQL para Render (usando variables de entorno separadas)
spring.datasource.url=jdbc:postgresql://dpg-d3135ivdiees73afmjag-a.oregon-postgres.render.com:5432/productosdb_aiv9
spring.datasource.username=${DB_USERNAME:productosdb_aiv9_user}
spring.datasource.password=${DB_PASSWORD:yNL9gzHySMsNx4gp4MmVdafbwflS9TmF}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.show-sql=false

# URLs de otros microservicios en Render
    stockservice.url=${STOCK_SERVICE_URL:https://stockservice-wki5.onrender.com}

# Endpoints de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Configuración de logging
logging.level.root=INFO
logging.level.com.inventario=INFO

# CORS para permitir acceso desde diferentes dominios
cors.allowed-origins=${CORS_ORIGINS:*}
